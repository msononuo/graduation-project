# Google OAuth (required for "Continue with Google")
# See GOOGLE-SETUP.md in the project for full steps.
# 1. Create OAuth 2.0 Client ID (Web application) at https://console.cloud.google.com/apis/credentials
# 2. In "Authorized JavaScript origins" add the EXACT URL you use (e.g. http://localhost:1573).
#    If Vite uses another port (1574, 1575), add that URL too.
# 3. Paste your Client ID below (no quotes).
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Backend: database (optional; defaults to local project DB on port 5433).
# If PostgreSQL requires a password, set DATABASE_URL with it (use URL-encoding for special chars, e.g. @ -> %40):
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5433/Graduation%20Project
